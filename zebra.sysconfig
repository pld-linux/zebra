# Customized setings for zebra

# Nice level for routing daemon [-10 - +10]
SERVICE_RUN_NICE_LEVEL="+2"

# Run BGP deamon ? [yes, no]
RUN_BGPD="no"

# Run OSPF for IPv6 daemon ? [yes, no]
RUN_OSPF6D="no"

# Run OPDF daemon ? [yes, no]
RUN_OSPFD="no"

# Run RIP daemon ? [yes, no]
RUN_RIP="no"

# Run RIPng daemon ? [yes, no]
RUN_RIPNGD="no"
